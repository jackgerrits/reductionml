[workspace]

members = [
    "reductionml-core",
    "reductionml-cli",
    "reductionml-python",
    "mdbook-reduction-config",
    "utils/reductionml-docs-extension"
]

default-members = [
    "reductionml-core",
    "reductionml-cli",
]

[profile.release]
lto = "thin"
